Grog:
Error → catch → understand why → 
form lesson → never same error again → 
accumulate wisdom → get smarter over time


chain is now: Gemini api → Grok api → Cerebras api → Minimalist Recovery → Grog Bypass no api relying on that died try new don't try other again does not work.  

