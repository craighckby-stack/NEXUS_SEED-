/**
 * Frontend Design Skill
 *
 * Comprehensive skill for transforming UI style requirements into production-ready frontend code
 * with systematic design tokens, accessibility compliance, and creative execution.
 *
 * @author [Your Name]
 * @since [Current Date]
 * @version [Current Version]
 */

/**
 * Skill Location
 *
 * @description Path to the skill files within the project context
 * @type {string}
 */
const skillPath = '{project_path}/skills/frontend-design/';

/**
 * Documentation
 *
 * @description Complete methodology and guidelines for frontend development
 * @type {object}
 */
const documentation = {
  skillMD: skillPath + 'SKILL.md',
  readMeMD: skillPath + 'README.md',
  license: skillPath + 'LICENSE',
};

/**
 * CSS Examples
 *
 * @description Production-ready frontend code for CSS, including tokens, components, and utilities
 * @type {object}
 */
const cssExamples = {
  tokens: skillPath + 'examples/css/tokens.css',
  components: skillPath + 'examples/css/components.css',
  utilities: skillPath + 'examples/css/utilities.css',
};

/**
 * TypeScript Examples
 *
 * @description Production-ready frontend code for TypeScript, including design tokens, theme provider, and sample components
 * @type {object}
 */
const typeScriptExamples = {
  designTokens: skillPath + 'examples/typescript/design-tokens.ts',
  themeProvider: skillPath + 'examples/typescript/theme-provider.tsx',
  sampleComponents: skillPath + 'examples/typescript/sample-components.tsx',
  utils: skillPath + 'examples/typescript/utils.ts',
};

/**
 * Templates
 *
 * @description Config files and global styles for the project
 * @type {object}
 */
const templates = {
  tailwindConfig: skillPath + 'templates/tailwind.config.js',
  globalsCss: skillPath + 'templates/globals.css',
};

/**
 * Quick Start
 *
 * @description Step-by-step guide for implementing the frontend design skill
 * @type {array}
 */
const quickStart = [
  {
    when: 'Building websites, web applications, or web components',
    chooseDesignDirection: 'Minimal SaaS, Bold Editorial, Soft & Organic, Dark Neon, Playful',
    generateDesignTokens: 'using the token system',
    buildComponents: 'using the provided examples',
    composePages: 'from components',
    reviewValidate: 'against the checklist',
  },
];

/**
 * Installation
 *
 * @description Step-by-step guide for setting up the skill in the project
 * @type {array}
 */
const installation = [
  {
    cmd: 'npm install -D tailwindcss postcss autoprefixer',
    desc: 'Install dependencies',
  },
  {
    cmd: 'npm install clsx tailwind-merge',
    desc: 'Install additional dependencies',
  },
  {
    cmd: 'npx tailwindcss init -p',
    desc: 'Initialize Tailwind',
  },
  {
    cmd: 'cp templates/tailwind.config.js ./tailwind.config.js',
    desc: 'Copy Tailwind configuration',
  },
  {
    cmd: 'cp templates/globals.css ./src/globals.css',
    desc: 'Copy global styles',
  },
];

/**
 * Design Tokens System
 *
 * @description Overview of the design tokens system used in the skill
 * @type {object}
 */
const designTokens = {
  colors: ['--background', '--surface', '--text', '--primary', '--secondary', '--accent'],
  typography: [
    '--font-size-{xs, sm, base, lg, xl, 2xl, 3xl, 4xl, 5xl}',
    '--line-height-{tight, snug, normal, relaxed, loose}',
    '--font-weight-{light, normal, medium, semibold, bold}',
  ],
  spacing: ['--spacing-{0.5, 1, 2, 3, 4, 6, 8, 10, 12, 16, 20, 24, 32, 40, 48}'],
};