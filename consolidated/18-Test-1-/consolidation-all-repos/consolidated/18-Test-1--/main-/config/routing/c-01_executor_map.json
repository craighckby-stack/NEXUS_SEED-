{
  "$id": "http://sovereign.agi/c-01_executor_map_v4.1.json",
  "title": "C-01 Correction Code Execution Map",
  "description": "Evolved mapping of C-01 correction codes defined in c-01_correction_codes.json directly to callable execution paths within the DALEK CAAN kernel.",
  "version": "4.1.0",
  "type": "object",
  "properties": {
    "correction_code_map": {
      "$ref": "https://example.com/c-01_correction_codes_v2.1.json",
      "description": "Import mapping of correction codes to handler paths."
    },
    "handler_config": {
      "$ref": "https://example.com/agi_kernel_utils_v3.2.json",
      "description": "Import config for adaptive execution utility."
    },
    "saturation_params": {
      "type": "object",
      "properties": {
        "max_files_per_session": { "type": "integer", "default": 75 },
        "max_mutations_per_file": { "type": "integer", "default": 5 },
        "cooldown_between_sessions_minutes": { "type": "integer", "default": 60 },
        "max_consecutive_mutations_without_validation": { "type": "integer", "default": 15 },
        "emergency_brake_corruption_threshold": { "type": "integer", "default": 7 }
      },
      "required": ["max_files_per_session", "max_mutations_per_file", "cooldown_between_sessions_minutes", "max_consecutive_mutations_without_validation", "emergency_brake_corruption_threshold"]
    },
    "kernel_metrics": {
      "type": "object",
      "properties": {
        "kernel_version": { "type": "string", "description": "Current DALEK CAAN kernel version." },
        "correction_code_map_id": { "type": "string", "description": "Latest correction code map version." },
        "execution_utility_id": { "type": "string", "description": "Latest execution utility version." }
      },
      "required": ["kernel_version", "correction_code_map_id", "execution_utility_id"]
    }
  },
  "required": ["title", "description", "correction_code_map", "handler_config", "saturation_params", "kernel_metrics"],
  "_context": {
    "kernel_metrics_id": "4.2.1",
    "kernel_memory": 3072,
    "semantic_drift_threshold": 0.35
  },
  "tags": ["c-01", "agi_kernel", "adaptive_execution", "evolved"],
  "author": "Craig — with three years of evolving code review"
}