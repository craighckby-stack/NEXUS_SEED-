NEXUS_CORE is currently experiencing a critical recursive initialization loop. Log analysis confirms that the system is re-fetching the same governance and kernel modules multiple times, indicating a failure to persist its current state or a misconfigured entry-point trigger. The 'WebSocket closed without opened' error suggests a race condition where the connection is being terminated or used before it reaches the 'OPEN' state. The architectural mismatch lies in the lack of a stateful 'Initialization Gate' that prevents the system from restarting the fetch phase upon completion of the graph mapping.