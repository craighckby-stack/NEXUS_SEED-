errors in code that needed fixing

* **Invalid `jsx` Attribute:** The `<style>` tag included a `jsx` prop (e.g., `<style jsx>`). This is a specific feature of Next.js (`styled-jsx`), but in standard React, it is treated as a non-boolean attribute being passed to a native DOM element.
* **Invalid `global` Attribute:** Similar to the above, the `global` prop on the `<style>` tag is not a standard HTML attribute and caused React to warn about writing non-standard data to the DOM.
* **DOM Property Mismatch:** React expects DOM attributes to be valid standard HTML properties; passing `true` (a boolean) to an attribute the browser expects to be a string (like a custom property name) triggers the specific "Received `%s` for a non-boolean attribute" warning.

the core recursive bridge logic (the actual "brain" that communicates with Gemini and commits to GitHub) was truncated into a placeholder.

due to the logic this is perfectly normal.

v1 how ever introduces hotswap
so user can automated instead of continually copy paste like me for the last 300 versions. 


just be prepared to copy and paste if the logic fails . 
